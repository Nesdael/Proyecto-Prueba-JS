{
  "name": "cinemax-spa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
